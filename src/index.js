import React from 'react'; // help to build all libraries
import ReactDOM from 'react-dom'; // help to render all our code on browser
import App from './App';

ReactDOM.render(
    <App/>, document.getElementById('root'));
